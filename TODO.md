# TODO — radiosoma

## Open issues

None open.

## Gaps

- [ ] No type-checker gate (mypy/pyright). Code carries inline hints but nothing verifies them in CI.
- [ ] No `ruff` config file checked in; lint relies entirely on the shared workflow defaults.
- [ ] `.coverage` artifact is committed in the repo root; consider adding to `.gitignore`.

## Code TODOs

None found.
